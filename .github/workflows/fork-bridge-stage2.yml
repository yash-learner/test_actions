name: Fork Bridge Stage 2

on:
  workflow_run:
    workflows: ["Fork Bridge Stage 1"]
    types: [completed]

permissions:
  actions: write   # read: download Stage 1 artifact; write: workflow_dispatch Stage 3

jobs:
  recover:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - name: workflow_run context + THE KEY ASSERTION (secret present here?)
        env:
          DUMMY: ${{ secrets.TEST_FORK_SECRET }}
        run: |
          echo "triggering event = ${{ github.event.workflow_run.event }}"
          echo "head_repository  = ${{ github.event.workflow_run.head_repository.full_name }}"
          echo "base repository  = ${{ github.repository }}"
          echo "head_branch      = ${{ github.event.workflow_run.head_branch }}"
          echo "stage1 run id    = ${{ github.event.workflow_run.id }}"
          if [ -n "$DUMMY" ]; then
            echo "STAGE2 secret: PRESENT ✅  (bridge recovered it, even on a fork)"
          else
            echo "STAGE2 secret: EMPTY ❌"
          fi

      - name: Download Stage 1 context
        uses: actions/download-artifact@v4
        with:
          name: review-context
          run-id: ${{ github.event.workflow_run.id }}
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Recover + validate context (treat artifact as untrusted)
        run: |
          PR=$(cat pr_number.txt); CID=$(cat comment_id.txt)
          echo "recovered PR      = $PR"
          echo "recovered comment = $CID"
          [[ "$PR"  =~ ^[0-9]+$ ]] || { echo "::error::bad PR number"; exit 1; }
          [[ "$CID" =~ ^[0-9]+$ ]] || { echo "::error::bad comment id"; exit 1; }
          echo "comment body:"; base64 -d comment_body_b64.txt; echo

      # workflow_dispatch is the recursion-guard exception, so GITHUB_TOKEN can fire Stage 3
      - name: Dispatch Stage 3 (gh-aw agent) with recovered ids
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          PR=$(cat pr_number.txt); CID=$(cat comment_id.txt)
          [[ "$PR"  =~ ^[0-9]+$ ]] || { echo "::error::bad PR number"; exit 1; }
          [[ "$CID" =~ ^[0-9]+$ ]] || { echo "::error::bad comment id"; exit 1; }
          # PR/CID are digit-validated above, so --argjson cannot inject.
          AW_CONTEXT=$(jq -nc \
            --argjson run_id "${{ github.event.workflow_run.id }}" \
            --arg repo "${{ github.repository }}" \
            --arg workflow_id "fork-bridge-stage2" \
            --arg item_type "pull_request" \
            --argjson item_number "$PR" \
            --argjson comment_id "$CID" \
            '{run_id:$run_id, repo:$repo, workflow_id:$workflow_id, item_type:$item_type, item_number:$item_number, comment_id:$comment_id}')
          gh workflow run fork-bridge-agent-awctx.lock.yml \
            --repo "${{ github.repository }}" \
            --ref "${{ github.event.repository.default_branch }}" \
            -f aw_context="$AW_CONTEXT"
          echo "dispatched Stage 3 (aw_context variant) for PR #$PR comment $CID"
